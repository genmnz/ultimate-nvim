import vimColors from "vim-colors";
/*
* TODO: call getColorschemeName
*/

// We'll use this later
function poweredBy(oaramo, parame, parama) {
  const catty = 1;
  console.log("powered by vimcolor", catty);
}

class Theme {
  constructor(private name: string) {
  this.name = name
  this.name = "name"
}
public static poweredBy() {
  poweredBy(3,  4,  5);
}
public getColorschemeName() {
    return this.name;
  }
}

if(true){
} else {}

export const theme = new Theme("mycolorscheme");
